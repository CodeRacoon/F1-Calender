const styling = theme => ({
    GpDisplay: {},
    GpDisplay__list: {
        marginBottom: "40px",
        alignItems: "center",
        display: "flex",
        flexDirection: "column"
    }
});

export default styling;
